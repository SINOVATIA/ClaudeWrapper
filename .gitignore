# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# PyInstaller build outputs (keep build/*.spec and build/entry.py)
build/dist/
build/work/

# Local logs / scratch
*.log

# Windows shortcuts — machine-specific (hard-coded paths to a local .venv)
*.lnk

# Claude Code local (per-user) settings — keep personal permission grants out
.claude/settings.local.json
