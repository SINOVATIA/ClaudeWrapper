[project]
name = "claude-wrapper"
dynamic = ["version"]
description = "MCP server that wraps the Claude Code CLI and exposes it as a callable service."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0",
]
test = [
    "pytest>=8.0",
]

[project.scripts]
claude-wrapper = "claude_wrapper.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Single source of truth for the version: src/claude_wrapper/__init__.py.
[tool.hatch.version]
path = "src/claude_wrapper/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/claude_wrapper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
